# Test task for int20h hackaton 2019

# Install 
```sh
npm install --prefix client/
npm install --prefix server/
```

# Build
```sh
npm run build --prefix client/
```

# Run
```sh
node server/src/index.js
```

**After all, Check localhost:3000**